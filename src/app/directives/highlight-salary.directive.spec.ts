import { HighlightSalary } from './highlight-salary.directive';

describe('HighlightSalary', () => {
  it('should create an instance', () => {
    const directive = new HighlightSalary();
    expect(directive).toBeTruthy();
  });
});
